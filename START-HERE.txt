BYLT dashboard -- update 5
==========================

UPLOAD THESE 4 FILES to your GitHub repo, replacing the existing ones:

    index.html
    app.js
    style.css
    README.md

GitHub -> Code -> Add file -> Upload files -> drag -> Commit changes.
Then Netlify -> Deploys -> Trigger deploy.

(Do NOT upload this START-HERE.txt.)

This replaces updates 3 and 4. If you have not uploaded those
yet, just use this one.


WHAT CHANGED
------------
REMOVED
  - The "215 orders are partially shipped..." note.
  - The two "how long shipping takes" panels.
  - The two "how long the queue has been waiting" panels.
  - The "of which partial" column from both channel tables.
  - "Show how this is counted" links (removed in update 4).

ADDED TO THE CARDS INSTEAD
  Two lines each, replacing four whole panels:
    Typical time to ship    3 days
    Waiting over 8 days     1,128

FIXED
  The per-day charts now print the number above each bar.

NEW ORDER
  1. Hero tiles
  2. Sales orders / Transfer orders cards
  3. Sales orders by channel
  4. Transfer orders by channel
  5. Sales orders shipped per day
  6. Transfer orders shipped per day
  7. Update these numbers (collapsed)


WHERE THE DETAIL WENT
---------------------
Partial orders still exist -- 215 of them. They count as shipped
and change no total, so they are off the page but kept as a
filterable column in the Export proof ledger.

The counting rules are inside the collapsed "Update these
numbers" panel, and on the Summary & Method sheet of the export.
