BYLT dashboard -- update 12
===========================

UPLOAD THESE 10 FILES to your GitHub repo, replacing existing:

    integrations.html
    integrations-export.js
    index.html
    integration-health.html
    load-data.html
    totals.html
    app.js
    style.css
    README.md
    package.json

Do NOT upload this START-HERE.txt.

Replaces update 11 -- use this one instead.


WHAT CHANGED
------------
The backlog chart now covers EVERY unshipped order, and the
total ties to the card above it.

Two rows added:
    Older        everything before the 18-day window
    Not due yet  pre-orders dated after today
    All orders   the total, matching the card exactly

The headline now reads "orders not shipped" and shows the full
figure (12,428), not the subset (6,622). That was the number
that didn't match.

Also fixed: the bands were alternating Cleared / Part way /
Cleared because one day sat at 94.3%, just under the old 95%
threshold. Lowered to 93%, so a clean stretch stays one band.


HOW TO CHECK IT
---------------
On each backlog section, the "All orders" row should equal
"Not shipped" on the card above. If it doesn't, something is
wrong -- tell me.


THEN DEPLOY TO PRODUCTION
-------------------------
1. Push these files, let dev redeploy, check the numbers tie
2. Netlify -> Add new project -> Import existing project
   -> GitHub -> pick your existing repo
3. Build command: BLANK.  Publish directory: BLANK or a dot.
   Functions directory: netlify/functions
4. Project configuration -> Environment variables, add BOTH:
       SITE_PASSWORD    the password people type
       SESSION_SECRET   any long random string
   Both required. Without SESSION_SECRET nobody can log in.
5. Deploys -> Trigger deploy -> Deploy site
6. Sign in, then load the files once:
       Order Status         2 files
       Integrations Status  4 files
   Blob storage is per-site, so production starts empty.
7. Project configuration -> General -> Change project name

Do NOT push over an existing production site that serves the
inventory reconciliation dashboard -- that would replace
index.html. Make a new site instead.
